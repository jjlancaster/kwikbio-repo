{
  "name": "scicrush-functions",
  "version": "0.1.0",
  "private": true,
  "engines": { "node": "18" },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.2.0",
    "firebase-functions": "^5.0.1",
    "stripe": "^16.2.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20"
  }
}
